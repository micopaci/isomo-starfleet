# ── Gradle caches and build outputs ──────────────────────────────────────────
.gradle/
build/
app/build/

# ── Machine-specific SDK path ─────────────────────────────────────────────────
local.properties

# ── JS/RN files that live in packages/mobile root, not here ──────────────────
# (these are accidentally committed copies — keep them out going forward)
app.json
index.js
babel.config.js
metro.config.js
tsconfig.json
react-native.config.js
preview.html
run-android.command
README.md
src/

# ── Signing keystores (except the committed debug keystore) ───────────────────
*.jks
# app/debug.keystore is intentionally tracked (public debug key)
